# planmgr

모바일 중심 서비스를 개발하는 프로젝트입니다. 가급적 Apple App Store 와 Google Play 양쪽에 등록 가능한 **네이티브 또는 하이브리드** 앱으로 만드는 것이 기본 방향입니다.

## 에이전트 팀 구성

이 저장소에는 4개의 전문 서브에이전트가 정의되어 있습니다. 작업 성격에 맞는 에이전트에게 위임하세요.

| 에이전트 | 역할 | 호출 시점 |
|---|---|---|
| `ux-creator` | 기획 + UX/UI 디자인 | 새 기능 기획, 화면 설계, 사용성 개선 |
| `developer` | 프론트엔드(모바일) + 백엔드 구현 | 코드 작성, 버그 수정, 리팩토링 |
| `infra` | 클라우드/CI-CD/배포 (선택적) | 서버, 인증, 푸시, 배포 자동화가 필요할 때만 |
| `qa` | 검수, 테스트, 마켓 정책 점검 | 구현 완료 후, 릴리스 전 |

## 표준 워크플로우

1. **기획** — `ux-creator`가 PRD, 유저 플로우, 화면 명세, 인수 기준 작성 → `docs/plan/`, `docs/design/`
2. **인프라 필요성 평가** — `infra`가 서비스 형태에 따라 인프라 사용 여부 결정 (불필요시 스킵)
3. **구현** — `developer`가 명세 기반으로 모바일 앱 + 백엔드 구현
4. **검수** — `qa`가 인수 기준, 마켓 정책, 접근성, 성능 점검
5. **수정 반복** — QA 피드백을 `developer`가 반영
6. **배포** — `infra`(있는 경우)가 빌드/배포 파이프라인으로 마켓 등록

## 핵심 원칙

- **모바일 우선**: 모든 설계와 구현은 모바일(iOS/Android)을 기준으로 한다.
- **마켓 정책 준수**: Apple App Store Review Guidelines, Google Play 정책을 항상 사전에 검토한다.
- **인프라는 선택적**: 서비스 형태에 따라 인프라 없이도 동작 가능하면 굳이 도입하지 않는다.
- **명세 기반**: 에이전트 간 협업은 `docs/` 하위 산출물을 기준으로 한다.

## 디렉토리 규칙

```
docs/
  plan/        # 기획 문서 (PRD, 유저 스토리)
  design/      # 디자인 명세, 화면 정의
  flow/        # 유저 플로우
  qa/
    scenarios/ # 테스트 시나리오
    bugs/      # 버그 리포트
    reports/   # 검수 결과
infra/         # IaC (필요한 경우)
.github/workflows/  # CI/CD
```
